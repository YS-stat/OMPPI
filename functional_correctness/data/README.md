# Functional-correctness input data

`humaneval_plus_generated_outputs_final.csv` contains the generated completions and precomputed full and partial evaluator outcomes used by the HumanEval+ experiment. `data_config.json` records the corresponding generation and evaluation configuration.

The released inference code does not execute generated programs. The implementation used to generate the raw input data is available from the authors upon request.
