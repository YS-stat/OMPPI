# Human-preference input data

`final_aligned_with_vertex_online.pkl` is the processed input table used by the human-preference experiments. It contains Chatbot Arena labels, prompts, responses, and aligned LLM-judge outputs.

The implementation used to generate the raw input data is available from the authors upon request.
